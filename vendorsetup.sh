# apply patches
#workdir=$(pwd)
#d hardware/qcom/audio-caf/sdm845 || return
#it reset -q --hard
# "$target" == "grus" ] && patch --merge -sNp1 -i "$workdir"/device/xiaomi/grus/patches/libtinycompress.patch
#d "$workdir" || return

# end patches


add_lunch_combo aicp_grus-userdebug

